- Add script to remove commits by list of commit message regexp.

- Add script to remove commits by list of tree path pattern.

- Skip `.git` directories inside worktree directories (with `.git` file instead of `.git` directory)

- Detect not pushed commits for remotes in `git-status.sh` script
